<Window x:Class="ApStatisticsApplicationUI.GeometricProbabilityAnswerSheet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApStatisticsApplicationUI"
        mc:Ignorable="d"
        Title="GeometricProbabilityAnswerSheet" Height="600" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="5*"/>
            <RowDefinition/>
           
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"  Text="Geometric Probability" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30"/>
        <TextBlock x:Name="WorkDisplay" Grid.Row="1" Padding="15" Text="Hello World" FontSize="15" />
        <Button x:Name="BackButton" Click="BackButton_Click" Grid.Row="2" Content="Back" FontSize="30" Background="Bisque"/>
        
    </Grid>
</Window>
